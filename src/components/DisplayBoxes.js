import React from 'react';

const DisplayBoxes = ({ distanceTraveled, remainingDistance }) => (
  <div>
    <div.info>Distance Traveled: {distanceTraveled} ft</div>
    <div.info>Remaining Distance: {remainingDistance} ft</div>
  </div>
);

export default DisplayBoxes;
